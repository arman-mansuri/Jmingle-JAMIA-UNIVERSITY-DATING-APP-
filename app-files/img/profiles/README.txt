Put your actual profile photos here, named to match the `image` field
in profile-data.js (e.g. img1.jpeg, img2.jpeg, img3.jpeg, img4.jpeg).

Until real photos are added, the sticker will just show a grey box
where the photo would be (the .profile-hotspot-img background-color
fallback in profile-hotspot.css).
